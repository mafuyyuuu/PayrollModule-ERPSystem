import { Box, Typography } from "@mui/material";

export default function ManagerPendingRequest() {
    return (
        <Box>
            <Typography variant="h4" mb={2}>
                Pending Request
            </Typography>
            <Typography>
                This is a placeholder for pending request.
            </Typography>
        </Box>
    );
}